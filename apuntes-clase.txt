apunte de la clase 
niveles de seguridad
1) se requiere usuario y contraseña 
2) evitemos inyecciones de sql

select * from usuarios where 
email='joselito@badmilton.com' and 
clave = 'joselito';

inyeccion en la celda de clave:
' or '1'='1
por logica
por eliminancion de simbolos str_replace

3) claves ocultas
encriptacion 
MD5
El profe va a utilizar password
metodos de encriptacion no reversibles, md5 es un algoritmo, 
'12345' con md5
827ccb0eea8a706c4c34a16891f84e7b
'1234' con md5
81dc9bdb52d04dc20036dbd8313ed055
no se parecen, son muy diferentes, no se pueden revertir porque se pierden bits al hacer las conversiones

4 puntos por problema de logica resuelto
100 por entrar a mindcontest

CLASE LUNES

login funciona
registro se hara con el gmail, al gmail se enviará la contraseña
en index:
desde el doctype hasta donde termina la barra de navegacion se corta, se crea un nuevo recurso, pegamos lo que cortamos y guardamos con el nombre head.php
regresamos al index y ponemos: <?php incluide "head.php"; ?>
hacemos lo mismo en login cuidando el corte, en recuperarmcontraseña tambien lo hacemos, tambien en registro

en phpmyadmin hace que por cada table de la base de datos generan un crud para insertar datos

creamos un nuevo recurso llamada class_usuario.php, se guarda en la carpeta de clases
<?php
class Usuario{
function ejecuta($accion){
switch($accion){
case 'login';

	break;
case 'registro';
	break;

	}
		  }
}
?>
modelo vista controlador MVC
todo lo que tenga que ver con la bd es el modelo
la vista es lo que desplegamos
el controlador es el como funciona 
la vista es el login, el controlador sera class_usuario y javascript

asi debe de quedar el nuevo login:
<?php
$oUsuario=new Usuario();
$error=oUsuario(ejecuta);
include 'class/class_basedatos.php';
include 'class/class_usuario.php';
$oUsuario=new Usuario();
$error=new $oUsuario -> ejecuta("login");
include "head.php"
?>
en el case login se pega lo que cortamos del login 
la linea que dice include class class se corta y se pega en el login antes de la linea obusuario
borrar: obbasededatos=new base de dattos
cambiar obbase de datos por this query

pesoshtml = pesos error 

antes de la llave que cierra la funcion le ponemos return html

mañana se hará recuperar contraseña

DESBLOQUEAR EL CORREO, clave segura para recuperar contraseña

